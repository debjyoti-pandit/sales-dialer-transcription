DEEPGRAM_API_KEY=dg_...

# Example: wss://sales-dialer-transcription-service.jp.ngrok.io/twilio/media
PUBLIC_WSS_URL=

# Server port for local dev (uvicorn --port can also override)
PORT=8001

# Optional (ngrok static domain requires token on your account)
NGROK_AUTH_TOKEN=
